# proof-manifest.toml
#
# Canonical register of proof numbers (test counts) that may be cited on the public
# site (index.html, GO-LIVE.md, case-studies/*.html, README.md, etc.).
#
# Each entry's `value` is the ONLY number that is allowed to be cited for that repo
# anywhere in committed site content. It is produced by ACTUALLY RUNNING the suite
# named in `source_cmd`, in `source_repo`, and reading the pytest summary line's
# "N passed" count (never "collected", never "passed + skipped" summed together).
#
# scripts/check_proof_numbers.py reads this file and fails a commit if any citation
# in site content contradicts it. See that script's module docstring for the exact
# matching/association rules.
#
# Key = the repo keyword as it is expected to appear in site text (e.g. "mcp-factory").

["mcp-factory"]
value = 222
source_repo = "C:\\Users\\jaime\\projects\\mcp-factory"
source_cmd = "python -m pytest -q"
# Neutralizes test_smoke_hub's live-bot auto-discovery on the fleet machine so
# the live-check reproduces the clean-checkout count that public CI gates on.
source_env = { MCP_FACTORY_SMOKE_ROOTS = "C:\\Users\\jaime\\projects\\mcp-factory\\tests\\fixtures\\empty_smoke_root" }
tail_line = "222 passed, 10 skipped in 5.13s"
verified_utc = "2026-07-22T22:55:00+00:00"
status = "verified"
note = "DRIFT 2026-07-22 (evening-ceremony preflight, ahead of the paired site refresh + factory push): 216->224->222 at commit 2491528 (ci: add GitHub Actions CI with README count-verification gate; repo local main ahead of origin by 1 commit at time of this check). Re-verified live in this pass. Prior: DRIFT 2026-07-20/21: 213->215 (2 tests added since 07-13, untraced, unrelated). Re-verified stable across 10+ independent runs after root-causing an unrelated apparent-flakiness report (213/215/195 across different invocations) down to a real bug in THIS checker's own live-check subprocess call -- see check_proof_numbers.py's live_verify_manifest() fix, 2026-07-21: it inherited GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE from git's own hook-invocation environment and leaked them into mcp-factory's test subprocess, corrupting its git-aware tests (test_workflow_runner.py) and producing a phantom 195. Fixed by stripping GIT_* env vars before spawning a different repo's test command. Prior: DRIFT 2026-07-13, 187->213."

["github-mcp"]
value = 97
source_repo = "C:\\Users\\jaime\\projects\\github-mcp"
source_cmd = "python -m pytest -q --tb=no"
tail_line = "97 passed, 1 skipped (junitxml: tests=98 failures=0 errors=0 skipped=1)"
verified_utc = "2026-07-23T14:55:11+00:00"
status = "verified"
note = "DRIFT FOUND 2026-07-23 (checker-parse-fix lane: the manifest's plain -q live-check had been silently WARN-skipping this entry since the summary line is suppressed under piped/non-tty capture -- fixed by teaching the live-check to parse junitxml instead, which then correctly caught this real drift that had been invisible behind the broken WARN). 86->97 since the 07-11 seed: 'ci: add GitHub Actions CI with README count-verification gate' (ec67472) added tests/test_check_readme_counts.py (+11 tests), 'harden(ci): SHA-pin third-party GitHub Actions' (5c3daf1) is a workflow-only change with no test delta. Cross-checked against the repo's own README Tests badge, which already reads '97 passing / 1 skipped' -- this is the manifest catching up to a repo that self-corrected. no .venv in this repo; ran with the global python per fallback rule. Prior: DRIFT FOUND 2026-07-11 (rate limiter fd0664c, 07-10, added 12 tests, 74->86)."

["desktop-mcp"]
value = 139
source_repo = "C:\\Users\\jaime\\projects\\desktop-mcp"
source_cmd = "python -m pytest -q --tb=no"
tail_line = "139 passed, 2 skipped (junitxml: tests=141 failures=0 errors=0 skipped=2)"
verified_utc = "2026-07-23T14:55:11+00:00"
status = "verified"
note = "DRIFT FOUND 2026-07-23 (checker-parse-fix lane, same class as github-mcp above: junitxml live-check fix caught real drift previously hidden behind a silent WARN). 128->139 since the 07-11 seed: 'ci: add GitHub Actions CI with README count-verification gate' (2607b94) added tests/test_check_readme_counts.py (+11 tests), matching the github-mcp pattern exactly; 'harden(ci): SHA-pin third-party GitHub Actions' (d3faac7) and the intervening docs/security commits are test-count-neutral. Cross-checked against the repo's own README Tests badge, which already reads '141 (139 passing, 2 skipped)' -- manifest catching up to a repo that self-corrected. no .venv in this repo; ran with the global python per fallback rule. Prior: DRIFT FOUND 2026-07-11 (121->128 since 07-09)."

["rag-mcp"]
value = 60
source_repo = "C:\\Users\\jaime\\projects\\rag-mcp"
source_cmd = ".venv/Scripts/python.exe -m pytest -q --tb=no"
tail_line = "60 passed (dot count; repo suppresses the pytest summary line, so live-check parses dots; matches the repo's own junit-counted 60/60 CI gate, public since 2026-07-22)"
verified_utc = "2026-07-23T14:30:00+00:00"
status = "verified"
note = "DRIFT FOUND 2026-07-11 (proof-numbers pre-publish audit): query-cap fix (000f23b) added 2 tests, 51->53. Re-verified 2026-07-11 -- value now 53, drift fixed on site + case study + README."

["bus-mcp"]
value = 99
source_repo = "C:\\Users\\jaime\\projects\\bus-mcp"
source_cmd = ".venv/Scripts/python.exe -m pytest -q --junitxml=<scratch>/bus.xml"
tail_line = "99 passed, 1 skipped (junitxml: tests=100 failures=0 errors=0 skipped=1)"
verified_utc = "2026-07-23T14:55:11+00:00"
status = "verified"
note = "DRIFT FOUND 2026-07-23 (checker-parse-fix lane: junitxml live-check fix caught real drift previously hidden behind a silent WARN -- this entry's own source_cmd already carried the <scratch> junitxml convention, which the fix now actually substitutes and parses instead of leaving unused). 62->99 since the 07-09 seed: 'ci: add GitHub Actions workflow + README count-verification gate' (acd5e5f) added tests/test_check_readme_counts.py (+11, same pattern as github-mcp/desktop-mcp), plus 'fix(security): validate lane param against path injection' (45a4f1a) and 'fix(security): gate 4 mutating tools behind default-OFF write flag' (186fc1f) each added their own dedicated test coverage for the remaining +26. Cross-checked against the repo's own README Tests badge, which already reads '100 (99 passing, 1 skipped)' -- manifest catching up to a repo that self-corrected. Still not cited as a proof number anywhere in site content (re-verified 2026-07-23: grepped index.html/README.md/GO-LIVE.md/case-studies/*/articles/*/scoreboard.html -- only bare repo-name mentions in fleet-reliability-day.html and fleet-audit-methodology.html, no numeric test-count citation either place). Tool count not re-checked this pass."

["options-bot"]
value = 371
source_repo = "C:\\Users\\jaime\\projects\\options-bot"
source_cmd = ".venv/Scripts/python.exe -m pytest -q"
tail_line = "353 passed, 1 skipped in 26.44s"
verified_utc = "2026-07-21T13:00:00+00:00"
status = "verified"
note = "DRIFT AGAIN 2026-07-20/21 (sixth rot since 07-06: 186->281->292->299->321->353): tastytrade price_effect sign-fix (+21 tests) and test-log-isolation fix (+2 tests) merged to master. Re-verified in options-bot's own .venv. Prior: DRIFT AGAIN 2026-07-19 (299->321)."

["discord-mcp"]
value = 160
source_repo = "C:\\Users\\jaime\\projects\\discord-mcp"
source_cmd = ".venv/Scripts/python.exe -m pytest -q"
tail_line = "160 passed, 1 skipped (junitxml: tests=161 failures=0 errors=0 skipped=1)"
verified_utc = "2026-07-23T14:55:11+00:00"
status = "verified"
note = "DRIFT FOUND 2026-07-23 (checker-parse-fix lane: junitxml live-check fix caught real drift previously hidden behind a silent WARN -- this entry's plain '-q' output is dots-only under piped capture, same suppressed-summary class as github-mcp/desktop-mcp/bus-mcp). 144->160 since the 07-21 04:48 UTC seed: 'Add create_message write tool (7th write tool, gated)' (6769e5c, 2026-07-21 09:00 local, i.e. AFTER that verification timestamp) added the 7th gated write tool plus its dedicated test coverage (+16). Cross-checked against the repo's own README Tests badge, which already reads '161 (160 passing, 1 skipped)' -- manifest catching up to a repo that self-corrected. Still not cited as a proof number anywhere in site content (re-verified 2026-07-23: grepped index.html/README.md/GO-LIVE.md/case-studies/*/articles/*/scoreboard.html -- only bare repo-name mentions in fleet-reliability-day.html and fleet-audit-methodology.html, no numeric test-count citation either place). Tool count not re-verified this pass -- no manifest field tracks it and this lane's scope is test counts only."

["reliability-retainer"]
value = 265
source_repo = "C:\\Users\\jaime\\projects\\reliability-retainer"
source_cmd = "python -m pytest -q --tb=no"
tail_line = "265 passed in 4.84s"
verified_utc = "2026-07-22T05:28:49+00:00"
status = "verified"
note = "Seeded 2026-07-13 for the new (unlinked, staged) case-studies/reliability-retainer.html page, which cites the repo's full-suite count. Live-verified at commit a4733c3 on branch slice5-runbook-sow-reconcile (unmerged slice1-5 work; no .venv in this repo, ran with the global python per the github-mcp/desktop-mcp fallback convention). 239 covers the whole repo (tools/, tests/, fleet-health-mcp/) -- not the same as the README's 'tests/ -- 39 tests' figure, which is a narrower subdirectory count from an earlier slice and is NOT cited on the site."

["rails-mcp"]
value = 106
source_repo = "C:\\Users\\jaime\\projects\\rails-mcp"
source_cmd = ".venv/Scripts/python.exe -m pytest -q"
tail_line = "106 tests (junitxml: tests=106 failures=0 errors=0 skipped=0)"
verified_utc = "2026-07-22T18:10:35+00:00"
status = "verified"
note = "8th flagship (governance category), went public/private-GitHub 2026-07-19. DRIFT 2026-07-22 (flagship-coverage lane, first time cited on site): 95 -> 106 (suite grew +11 since 07-19 seed; config/routes/server suites expanded). Live-verified stable across 2/2 junitxml runs in the repo's own .venv; repo's own README Tests badge already reads '106 passing', so this is the manifest catching up to a repo that self-corrected. Tool count live-verified at 6 (classify_action, is_action_registered, get_rails_hash, get_signoff_state, record_spend_intent, evaluate_budget). Now cited on case-studies/rails-mcp.html + index.html #cases card."

["vllm-ops-mcp"]
value = 95
source_repo = "C:\\Users\\jaime\\projects\\vllm-ops-mcp"
source_cmd = ".venv/Scripts/python.exe -m pytest -q"
tail_line = "95 passed, 7 skipped (junitxml: tests=102 failures=0 errors=0 skipped=7)"
verified_utc = "2026-07-22T18:10:35+00:00"
status = "verified"
note = "7th flagship, went public/private-GitHub 2026-07-19. DRIFT 2026-07-22 (flagship-coverage lane, first time cited on site): 76 -> 95 (unit suite grew +19 since 07-19 seed; the 7 live-infra smokes still skip without VLLM_OPS_MCP_LIVE=1). Live-verified stable across 2/2 junitxml runs in the repo's own .venv; repo's own README Tests badge already reads '102 (95 passing, 7 live-skipped)', so this is the manifest catching up to a repo that self-corrected. Per this manifest's convention the citable headline number is passed-only (95), not the 102 combined total. Tool count live-verified at 6 (check_health, get_gpu_status, get_serve_config, get_service_status, list_models, test_completion). Now cited on case-studies/vllm-ops-mcp.html + index.html #cases card."

["mcp-security-scanner"]
value = 337
source_repo = "C:\\Users\\jaime\\projects\\mcp-security-scanner"
source_cmd = "python -m pytest -q --tb=no"
tail_line = "337 passed, 9 skipped (junitxml: tests=346 failures=0 errors=0 skipped=9)"
verified_utc = "2026-07-23T20:00:00+00:00"
status = "verified"
note = "no .venv in this repo, ran with global python per the github-mcp/desktop-mcp fallback convention. Prior drifts: 44->48 (07-19), 48->59 (07-21), 82->101 (07-22 05:57). DRIFT AGAIN 2026-07-22 18:10 (flagship-coverage lane, first time cited on site): 101 -> 195 (JS/TS parity across .js/.mjs/.cjs/.ts/.mts/.cts/.jsx/.tsx + reachability-grading matrix + tool-parameter taint-tracking matrix all merged today; the 7 skips are the self-audit tests that need MCP_SCANNER_FLEET_ROOT). Live-verified stable across 2/2 junitxml runs. Repo's own README already reads '202 tests (195 passing, 7 self-audit skip)', so this is the manifest catching up to a repo that self-corrected. Now cited on case-studies/mcp-security-scanner.html + index.html #cases card. NOTE this repo is the fastest-drifting number on the site (5 rots since 07-19) -- live-re-verify before any public copy. DRIFT AGAIN 2026-07-23 (6th): 195 -> 257 passing / 264 total (tool-discovery low-level SDK support @ fe36864 + detector-5/6 low-level coverage @ 4a63544, both triple-review-gated [2x refuter N-vote + Opus final verify each], merged+pushed same morning). Live-verified via repo count-gate (scripts/check_readme_counts.py green at 264/257/7). DRIFT AGAIN 2026-07-23 ~15:20 (7th, same day): 257 -> 264 passing / 271 total (decorator-path import-aware gate/sink resolver @ 15b5460, 2x refuter N-vote + Opus final verify, pushed) -- folded into the checker-parse-fix deploy so site+repo land together. DRIFT AGAIN 2026-07-23 evening (8th, same day): 264 -> 337 passing / 346 total @ 88b133c (sink-substring resolution arc + fp-wave1 one-law FP-reduction, both N-vote + Opus-MERGE-SAFE final verify; README/ANNOUNCEMENT self-audit table rebuilt live). The self-audit narrative on the case study + index card was ALSO corrected this pass: 6 servers -> 8, and mcp-factory now scans CLEAN (codegen class fixed upstream 2026-07-21) with github-mcp/discord-mcp showing honest P1/LOW rows for their own fake test-fixture tokens -- so the old 'flags the codegen class, gives the other five a clean bill' copy is retired as factually stale. Live-verified 337/9 via py -3.12 pytest."
